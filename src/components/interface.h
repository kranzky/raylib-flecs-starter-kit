#ifndef INTERFACE_COMPONENT_H
#define INTERFACE_COMPONENT_H

#include "../defines.h"

#include <nuklear.h>
#include <flecs.h>

typedef struct nk_context Interface;
ECS_COMPONENT_DECLARE(Interface);

#endif
